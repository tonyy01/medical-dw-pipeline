# ============================================================
# dbt 项目配置 — 医疗数据仓库维度建模
# ============================================================

name: "medical_dw"
version: "1.0.0"
config-version: 2
profile: "medical_dw"

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "logs"

models:
  medical_dw:
    ods:
      +materialized: view
      +schema: ods
      +tags: ["ods"]

    dwd:
      +materialized: table
      +schema: dwd
      +tags: ["dwd"]
      dim_date:
        +materialized: table
        +tags: ["dwd", "dimension"]

    dws:
      +materialized: table
      +schema: dws
      +tags: ["dws"]

    ads:
      +materialized: table
      +schema: ads
      +tags: ["ads"]

seeds:
  +schema: ods
  +tags: ["seed"]
